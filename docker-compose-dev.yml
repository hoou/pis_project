version: '3.4'

services:

  shop:
    container_name: shop
    build:
      context: services/shop
      dockerfile: Dockerfile-dev
    volumes:
      - './services/shop:/usr/src/app'
    ports:
      - 5001:5000
    environment:
      - FLASK_APP=project/__init__.py
      - FLASK_DEBUG=1
      - APP_SETTINGS=project.config.DevelopmentConfig
      - DATABASE_URL=postgres://postgres:postgres@db:5432/pis_dev
      - DATABASE_TEST_URL=postgres://postgres:postgres@db:5432/pis_test
    depends_on:
      - db
    links:
      - db

  db:
    container_name: db
    build:
      context: services/shop/project/db
      dockerfile: Dockerfile
    ports:
      - 5435:5432
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres